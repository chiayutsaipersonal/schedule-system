module.exports = {
    captialize: (string) => { return string.charAt(0).toUpperCase() + string.slice(1); },
    decapitalize: (string) => { return string.charAt(0).toLowerCase() + string.slice(1); }
};
